// ===============circles============
var Circle = {
  createNew: function(x, y, id, group){
    var circle = {};
    circle.x = x;
    circle.y = y;
    circle.id = id;
    circle.group = group;
    return circle;
  }
}
